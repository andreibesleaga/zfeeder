{
  "name": "zfeeder-e2e",
  "private": true,
  "version": "2.0.0",
  "description": "Browser and accessibility tests for zFeeder 2.0",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0"
  }
}
